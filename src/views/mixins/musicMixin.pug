mixin music(music)
   div#audio__control(data-id=music._id)  
    a(href=`/${music._id}`)=music.title


    if loggedIn
      if  loggedInUser.playlist.includes(music.id) == false
       c#whiteHeart 🤍 
      else 
       b#redHeart 💗